{% extends '@Backend/base/crud/add.html.twig' %}
{% set crudIndexAction = 'MediaItemIndex' %}

{% block sidebarSubNavigation %}
  {% include '@MediaLibrary/Backend/NavigationTree.html.twig' with {'overview': true} %}
{% endblock %}

{% block content %}
  {{ form_start(backend_form) }}
  <div class="fallback">
    {{ form_widget(backend_form.vich) }}
  </div>
  {{ form_errors(backend_form) }}
  {{ form_rest(backend_form) }}

{#  <div class="btn-toolbar">#}
{#    {% if crudIndexAction is defined and is_allowed(crudIndexAction, crudIndexModule) %}#}
{#      <div class="btn-group" role="group">#}
{#        {{ macro.buttonIcon(action_path(crudIndexAction, crudIndexModule, crudIndexParameters, crudIndexRelative, crudIndexLocale), 'times', 'lbl.Cancel'|trans|ucfirst, 'btn-default') }}#}
{#      </div>#}
{#    {% endif %}#}
{#    <div class="btn-group ms-auto" role="group">#}
{#      {{ macro.buttonIcon('', submitIcon|default('plus-square'), submitLabel|default('lbl.Add'|trans|ucfirst), submitClass|default('btn-primary')) }}#}
{#    </div>#}
{#  </div>#}
  {{ form_end(backend_form) }}
{% endblock %}
